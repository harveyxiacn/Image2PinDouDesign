下面是一份偏“可落地研发”的完整设计文档，目标是做一个网站：用户上传一张或多张图片，选择 52 针、104 针等拼豆板规格，系统自动转换成拼豆设计图，并统计每张图及整个项目需要的豆子颜色和数量。色卡可基于你提供的 Mard Bead 色卡页面维护：`https://www.pixel-beads.com/zh/mard-bead-color-chart`。

---

# 拼豆设计图生成网站设计文档

## 1. 项目概述

### 1.1 项目名称

暂定：**Pixel Beads Designer / 拼豆图纸工坊**

### 1.2 项目目标

提供一个在线拼豆图纸生成工具，支持用户：

1. 上传图片。
2. 选择拼豆板尺寸，例如：
   - 52 针
   - 104 针
   - 自定义宽高
   - 多块拼豆板拼接
3. 自动将图片转换成拼豆图纸。
4. 使用真实拼豆色卡进行颜色匹配。
5. 生成每张图所需的颜色编号、颜色名称、豆子数量。
6. 支持多张图片组成项目，汇总所有图片需要的颜色和总数量。
7. 支持在线编辑图纸。
8. 支持保存、导出、打印、分享。

---

# 2. 核心用户场景

## 2.1 单张图片生成拼豆图纸

用户上传一张动漫、游戏、头像或照片图片，选择 52 x 52 拼豆板，系统生成：

- 拼豆网格图
- 每格对应色号
- 下方颜色图例
- 每种颜色需要多少颗豆子
- 可导出 PNG / PDF / Excel / CSV

## 2.2 多张图片组成项目

例如用户想做一套“游戏王拼豆”：

- 黑魔导
- 黑魔导女孩
- 栗子球
- 青眼白龙
- 真红眼黑龙

用户可以上传多张图片，每张生成一张拼豆图纸，系统汇总：

| 色号 | 颜色名称 | 总数量 |
| ---- | -------- | ------ |
| A01  | 黑色     | 3260   |
| A02  | 白色     | 2140   |
| A17  | 紫色     | 1890   |

方便用户一次性购买所有需要的豆子。

## 2.3 用户后期手动调整

自动生成的图纸可能存在细节不满意，用户可以：

- 替换单个格子的颜色
- 用画笔批量涂色
- 橡皮擦去除豆子
- 吸管选择已有颜色
- 限制只使用已有颜色
- 一键合并相近颜色
- 重新统计数量

---

# 3. 产品功能设计

## 3.1 功能总览

### MVP 必备功能

| 模块       | 功能                                           |
| ---------- | ---------------------------------------------- |
| 图片上传   | 支持单图、多图上传                             |
| 图片处理   | 裁剪、缩放、居中、透明背景处理                 |
| 拼豆板选择 | 52 针、104 针、自定义尺寸                      |
| 自动转换   | 图片像素化、颜色匹配、图纸生成                 |
| 色卡管理   | 使用 Mard Bead 色卡，维护色号、颜色名、RGB/HEX |
| 图纸预览   | 网格图、色号图、缩放查看                       |
| 数量统计   | 每张图统计颜色和数量                           |
| 项目汇总   | 多图合并统计总豆数                             |
| 保存项目   | 登录用户保存设计                               |
| 导出       | PNG、PDF、CSV、Excel                           |

### 进阶功能

| 模块       | 功能                                |
| ---------- | ----------------------------------- |
| 图纸编辑器 | 画笔、橡皮擦、吸管、填充、撤销/重做 |
| 颜色优化   | 限制颜色数量、合并近似色            |
| 抖动算法   | Floyd-Steinberg、Ordered Dithering  |
| 背景移除   | 自动识别背景，转换为空格或白豆      |
| 黑边增强   | 适合动漫图、像素风图                |
| 分板打印   | 104 针图纸按多个 52 针板分页        |
| 库存管理   | 用户记录自己已有豆子数量            |
| 购买清单   | 生成缺货颜色和数量                  |
| 分享社区   | 发布作品、点赞、收藏、评论          |
| 权限控制   | 私密项目、公开项目、链接分享        |

---

# 4. 关键概念定义

## 4.1 拼豆板 Board

系统内部将拼豆板抽象为一个二维网格。

例如：

```text
52 针板：52 x 52 = 2704 个格子
104 针板：104 x 104 = 10816 个格子
```

如果实际物理拼豆板不是正方形，例如某品牌为 29 x 29 或多块拼接，系统也应该支持自定义。

建议设计为：

```ts
BoardPreset {
  id: string;
  name: string;          // 52针、104针、自定义
  width: number;         // 横向针数
  height: number;        // 纵向针数
  physicalWidthMm?: number;
  physicalHeightMm?: number;
  brand?: string;
}
```

### 重要说明

“52 针”和“104 针”在不同拼豆板品牌中可能含义不完全一致，开发时建议做成可配置预设，而不是写死。

例如：

| 预设名称 |       宽 |       高 | 说明                  |
| -------- | -------: | -------: | --------------------- |
| 52 针    |       52 |       52 | 常用小图              |
| 104 针   |      104 |      104 | 大图                  |
| 52 x 104 |       52 |      104 | 长图                  |
| 自定义   | 用户输入 | 用户输入 | 任意尺寸              |
| 多板拼接 | N x M 块 | 自动计算 | 例如 2 x 2 块 52 针板 |

---

## 4.2 拼豆色卡 Palette

色卡数据应包含：

```ts
PaletteColor {
  id: string;
  paletteId: string;
  brand: string;       // Mard, Perler, Artkal等
  code: string;        // 色号，例如 A01、501、C123
  nameZh: string;      // 中文色名
  nameEn?: string;     // 英文色名
  hex: string;         // #RRGGBB
  rgb: {
    r: number;
    g: number;
    b: number;
  };
  lab: {
    l: number;
    a: number;
    b: number;
  };
  isAvailable: boolean;
  sortOrder: number;
}
```

### 色卡来源

你提供的页面：

```text
https://www.pixel-beads.com/zh/mard-bead-color-chart
```

可以用于维护 Mard Bead 色卡。

建议实现方式：

1. 初期人工整理为 CSV / JSON。
2. 后台支持导入 CSV。
3. 后续做定时同步或半自动抓取。
4. 注意版权、使用许可和来源标注。

示例 CSV：

```csv
brand,code,name_zh,name_en,hex
Mard,A01,黑色,Black,#101010
Mard,A02,白色,White,#FFFFFF
Mard,A03,红色,Red,#D92828
```

---

## 4.3 拼豆图纸 Design

一张上传图片转换后形成一个 Design。

```ts
Design {
  id: string;
  projectId: string;
  originalImageUrl: string;
  previewImageUrl: string;
  boardWidth: number;
  boardHeight: number;
  paletteId: string;
  matrix: number[][];        // 每个格子对应 paletteColorId 或空
  colorCounts: Record<string, number>;
  settings: ConversionSettings;
}
```

---

## 4.4 项目 Project

一个项目可以包含多张 Design。

例如“游戏王拼豆合集”就是一个 Project，里面包含 5 张图纸。

```ts
Project {
  id: string;
  userId: string;
  name: string;
  description?: string;
  designs: Design[];
  totalColorCounts: Record<string, number>;
}
```

---

# 5. 用户流程设计

## 5.1 单图生成流程

```text
进入网站
  ↓
点击「上传图片」
  ↓
选择图片
  ↓
进入图片设置页
  ↓
选择拼豆板规格：52针 / 104针 / 自定义
  ↓
选择色卡：Mard Bead
  ↓
设置选项：
  - 裁剪方式
  - 最大颜色数量
  - 是否启用抖动
  - 是否去背景
  - 是否增强黑边
  ↓
点击「生成图纸」
  ↓
后台转换
  ↓
显示结果：
  - 拼豆设计图
  - 色号图例
  - 数量统计
  ↓
用户可编辑
  ↓
保存 / 导出 / 加入项目
```

---

## 5.2 多图项目流程

```text
创建项目
  ↓
上传多张图片
  ↓
为每张图片设置板型和转换参数
  ↓
批量生成图纸
  ↓
进入项目详情页
  ↓
查看每张图纸
  ↓
查看项目总用豆统计
  ↓
导出：
  - 每张图纸 PDF
  - 总豆子采购清单 Excel
  - 项目压缩包
```

---

# 6. 页面设计

## 6.1 首页

### 主要区域

1. 顶部导航：

   - Logo
   - 在线生成
   - 我的项目
   - 色卡
   - 教程
   - 登录/注册
2. Hero 区域：

   - 标题：上传图片，一键生成拼豆图纸
   - 按钮：立即上传
   - 示例图：原图 → 拼豆图 → 豆子统计
3. 功能介绍：

   - 支持 52 针 / 104 针
   - 支持 Mard 色卡
   - 支持多图统计
   - 支持 PDF 打印

---

## 6.2 上传页

功能：

- 拖拽上传
- 点击上传
- 支持批量上传
- 支持格式：
  - JPG
  - PNG
  - WebP
  - HEIC 可选
- 显示上传进度
- 上传失败提示

限制建议：

| 项目           | 限制                        |
| -------------- | --------------------------- |
| 单张图片大小   | 20MB                        |
| 单次上传数量   | 免费用户 10 张，会员 100 张 |
| 图片最大分辨率 | 8000 x 8000                 |
| 支持格式       | jpg/png/webp                |

---

## 6.3 图片设置页

### 左侧

- 原图预览
- 裁剪框
- 旋转
- 翻转
- 缩放
- 背景透明预览

### 右侧

#### 拼豆板设置

| 参数     | 说明                       |
| -------- | -------------------------- |
| 板型     | 52针、104针、自定义        |
| 宽度     | 默认跟板型一致             |
| 高度     | 默认跟板型一致             |
| 适配方式 | 填充、完整显示、裁剪、拉伸 |
| 分板线   | 例如每 52 格显示一条粗线   |

#### 颜色设置

| 参数       | 说明                           |
| ---------- | ------------------------------ |
| 色卡品牌   | Mard Bead                      |
| 最大颜色数 | 8、16、24、32、48、无限制      |
| 保留透明   | PNG 透明区域不放豆             |
| 背景处理   | 白色 / 透明 / 指定色           |
| 抖动算法   | 无 / Floyd-Steinberg / Ordered |
| 黑边增强   | 适合动漫线稿                   |
